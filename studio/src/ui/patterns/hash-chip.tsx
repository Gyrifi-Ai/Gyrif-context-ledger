import { useState } from "react";
export function HashChip({ value, label }: { value: string; label?: string }) { const [copied, setCopied] = useState(false); const copy = async () => { try { await navigator.clipboard?.writeText(value); setCopied(true); window.setTimeout(() => setCopied(false), 1200); } catch { /* Clipboard is unavailable in non-secure contexts. */ } }; return <button type="button" title={value} onClick={() => void copy()} className="rounded-full border border-border bg-muted px-2 py-1 font-mono text-xs text-muted-foreground">{copied ? "Copied" : `${label ? `${label} · ` : ""}${value.slice(0, 10)}…`}</button>; }
