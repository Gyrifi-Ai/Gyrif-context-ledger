import type { ReactNode } from "react";
export function Panel({ title, eyebrow, description, actions, footer, padding = "default", children }: { title?: ReactNode; eyebrow?: string; description?: ReactNode; actions?: ReactNode; footer?: ReactNode; padding?: "none" | "default"; children: ReactNode }) { return <section className="overflow-hidden rounded-lg border border-border bg-card shadow-sm">{(title || eyebrow || description || actions) && <header className="flex items-start justify-between gap-4 border-b border-border p-4"><div>{eyebrow && <p className="text-[11px] font-semibold uppercase tracking-wider text-muted-foreground">{eyebrow}</p>}{title && <h2 className="font-semibold">{title}</h2>}{description && <p className="mt-1 text-sm text-muted-foreground">{description}</p>}</div>{actions}</header>}<div className={padding === "default" ? "p-4" : ""}>{children}</div>{footer && <footer className="border-t border-border p-4">{footer}</footer>}</section>; }
