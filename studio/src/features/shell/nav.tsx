import { Boxes, GitPullRequest, History, Inbox } from "lucide-react";
import type { Route } from "../../app/router";
import { cn } from "@/lib/utils";
const items: { id: Route; label: string; Icon: typeof Boxes }[] = [{ id: "ledgers", label: "Ledgers", Icon: Boxes }, { id: "changes", label: "Changes", Icon: Inbox }, { id: "proposals", label: "Proposals", Icon: GitPullRequest }, { id: "releases", label: "Releases", Icon: History }];
export function Nav({ route, ledgerId }: { route: Route; ledgerId: string }) { return <nav className="flex gap-1 overflow-x-auto md:grid">{items.map(({ id, label, Icon }) => { const disabled = id !== "ledgers" && !ledgerId; return <a key={id} href={disabled ? undefined : `#${id}`} aria-disabled={disabled || undefined} title={disabled ? "Select a ledger first" : undefined} className={cn("relative flex items-center gap-2 rounded-sm px-3 py-2 text-sm font-medium text-muted-foreground hover:bg-card hover:text-foreground", route === id && "bg-card text-foreground before:absolute before:left-0 before:h-4 before:w-0.5 before:rounded-full before:bg-primary", disabled && "cursor-not-allowed opacity-45") }><Icon className="size-4" strokeWidth={1.75} />{label}</a>; })}</nav>; }
