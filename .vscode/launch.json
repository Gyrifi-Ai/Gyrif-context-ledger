{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Gyrifi: Start local stack",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "docker compose up --build --remove-orphans"
    }
  ]
}
